import concepts.finance as finance;


select
    finance.organization.name,
    finance.organization.key,
    finance.total_amount
where
    finance.organization.name = "Canadian Division"
order by
    finance.total_amount desc
limit 100;