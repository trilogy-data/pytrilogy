# Datasource ingested from users_with_pk
# Generated on 2026-05-24 09:23:52.863604
import std.net;

key user_id enum<int>[1, 2, 3];

properties user_id (
    username enum<string>['alice', 'bob', 'charlie'],
    email enum<string>['alice@example.com', 'bob@example.com', 'charlie@example.com']::email_address,
    created_at datetime,
);

root datasource users_with_pk (
    user_id,
    username,
    email,
    created_at,
)
grain (user_id)
address users_with_pk;