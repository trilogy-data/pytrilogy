
parameter scale int;
parameter float float;
parameter string string;
parameter date date;
parameter dt datetime;
auto numbers <- generate_array(1, scale, 1);

select count(unnest(numbers))  as cnt, float, string, date, dt;