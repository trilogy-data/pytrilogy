import source_one;
import source_one as alt;

property x.extra string;

datasource x_extra_source (
   ~x,
    extra,
)
grain (x)
query '''
select 1 as x, 'a' as extra
''';

datasource x_alternate_source (
   ~alt.x,
)
grain(alt.x)
address test
state unpublished;