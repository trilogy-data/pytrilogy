# Pinned distinct pair span: observed traffic-source x category combinations.
import order_items;

where
    user.id is not null
    and product.id is not null
select
    user.traffic_source,
    product.category
order by
    user.traffic_source asc,
    product.category asc
;
