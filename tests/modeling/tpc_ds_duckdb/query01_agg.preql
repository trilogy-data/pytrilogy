import store_sales as ss;

show where
    ss.return_store.state = 'TN' and ss.return_date.year = 2000
select
    ss.return_customer.id,
    --sum(ss.return_amount) as total_returns,
order by
    ss.return_customer.id asc
limit 100
;
