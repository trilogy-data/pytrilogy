import unified_sales as sales;

property color_ids <- filter sales.item.name where sales.item.color in ('slate', 'blanched', 'burnished');

WHERE
    sales.item.name in color_ids
    and sales.date.year = 2001
    and sales.date.month_of_year = 2
    and sales.bill_address.gmt_offset = -5
SELECT
    sales.item.name,
    sum(sales.ext_sales_price) -> total_sales,
ORDER BY
    total_sales asc nulls first,
    sales.item.name asc nulls first
LIMIT 100
;
