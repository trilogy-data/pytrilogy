key id int;

properties id (
    string_id string,
    type string,
    carrier string?,
);

datasource ship_mode (
    SM_SHIP_MODE_SK: id,
    SM_SHIP_MODE_ID: string_id,
    SM_TYPE: type,
    SM_CARRIER: carrier,
)
grain (id)
address memory.ship_mode;
