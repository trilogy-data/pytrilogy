# Return reasons.

key sk int; # surrogate key

properties sk (
    id string, # business identifier
    desc string?, # Free-form, e.g. 'Did not fit', 'wrong color'.
);

datasource reason (
    R_REASON_SK: sk,
    R_REASON_ID: id,
    R_REASON_DESC: desc,
)
grain (sk)
address memory.reason;
