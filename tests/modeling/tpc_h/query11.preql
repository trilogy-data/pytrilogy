import part;

auto part_value <- sum(supply_cost * available_quantity) by id;
auto total_value <- sum(supply_cost * available_quantity) by *;

where supplier.nation.name = 'GERMANY'
then where part_value > total_value * 0.0001
select
    id,
    part_value,
order by
    part_value desc;
