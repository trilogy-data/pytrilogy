import customer;
import order as orders;

merge orders.customer.id into ~id;

auto orders_per_customer <- count(orders.id ? not orders.comment like '%special%requests%') by id;

select
    coalesce(orders_per_customer, 0) -> c_count,
    count(id) -> custdist
order by
    custdist desc,
    c_count desc;
