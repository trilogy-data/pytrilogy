pytest
pytest-cov
pytest-mock
duckdb<1.5.4
duckdb-engine
mypy
# 0.16.0 false-positives PYI041 on the build-dispatch union; fixed in 0.16.2
ruff>=0.16.2
# Pinned: black --check is a CI gate, and an unpinned formatter fails every
# open PR the day a new stable style ships.
black==26.5.1
pandas<3.0.0
types-tabulate
google-cloud-bigquery
sqlalchemy-bigquery
#fakesnow needs an upgrade to support 1.9; it calls CURRENT_VERSION which isn't mocked and hits duckdb
snowflake-sqlalchemy
fakesnow
matplotlib
networkx
tomli-w
types-tabulate
httpx
maturin
fastapi
uvicorn
altair
vl-convert-python
markdown
plotext
# chdb has no Windows wheels (macOS + Linux only); skip on win32
chdb; sys_platform != "win32"
clickhouse-sqlalchemy
