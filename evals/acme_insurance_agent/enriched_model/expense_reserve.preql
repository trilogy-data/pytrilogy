# Key-only marker: one row per claim_amount row that is an expense reserve. Nothing to select here; use claim_amount.is_expense_reserve.

key id int; # claim_amount identifier

root datasource expense_reserve (
    Claim_Amount_Identifier: id,
)
grain (id)
address Expense_Reserve;
