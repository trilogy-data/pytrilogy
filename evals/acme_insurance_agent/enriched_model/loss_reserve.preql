# Key-only marker: one row per claim_amount row that is a loss reserve. Nothing to select here; use claim_amount.is_loss_reserve.

key id int; # claim_amount identifier

root datasource loss_reserve (
    Claim_Amount_Identifier: id,
)
grain (id)
address Loss_Reserve;
