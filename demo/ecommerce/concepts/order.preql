## posts
import user as user;

key id int;

metric count <- count(id);

datasource orders (
    user_id: user.id,
    order_id: id,
    )
    grain (id)
    address bigquery-public-data.thelook_ecommerce.orders
;

