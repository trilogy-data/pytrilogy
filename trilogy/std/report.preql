def top_x_by_metric(val, metric, x=10, default='other') -> CASE
    WHEN rank(val) over (order by metric desc) < x + 1 THEN val
    ELSE default
END; # filter a list of values to the top x by a given metric, replacing the rest with a default value