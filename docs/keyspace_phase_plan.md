# Project plan: a keyspace phase in discovery

> **Where this stands (2026-09-25): phases 0-5, owner answer 5 (a filter is a value), a review cleanup pass and phases 6-7 step 1 (every demanded region gets a domain; the region contract on nodes, resolved datasources and join typing) are BUILT, on branch `extension-row-null-semantics`, draft PR #702. Phases 6-7 step 2 (delete the election and the assembly-side patches the contract made redundant) remains, on the same PR. A fresh session should read "The problem" and "The idea" for the vocabulary, then jump to [Status](#status) and start from "Pick up here".** The sections before Status are the original plan, left unedited on purpose: where they say "nothing here is built" or list a site as re-deriving its own answer, Status is the truth.

Written 2026-09-20, from scratch, for a fresh session. It proposes one new analysis in v4 discovery and a strangler migration onto it. Nothing here is built. Background: `docs/handoff_extension_row_semantics.md` (the rule this serves, and a prototype that was backed out), `docs/extent_ownership.md`, `docs/domain_graph_design.md`.

## The problem

Every statement has a **row universe**: the kinds of row it can return, and which entities exist on each kind. Customers and orders, not every customer has an order, `select customer_id, status`: two kinds of row. One has a customer AND an order. One has a customer and no order.

Nothing in discovery computes that. Instead about a dozen places each re-derive the fragment they need, from stamps (`Modifier.PARTIAL`, `partial_concepts`, `nullable_concepts`) and from the shape of whatever was already built:

| where | the fragment it answers |
|---|---|
| `partial_bridging.heal_pinned_partials` (before discovery, rewrites datasources) | does this WHERE empty a kind of row? |
| `join_resolution.licensed_extension_spans`, `extent_ownership.spans_demanded_by` / `span_members` | which extra kinds of row does the statement ask for? |
| `extent_ownership.elect_extent_owners` (END of `build_group_graph`, after groups exist) | which group produces them? |
| `group_graph._keep_extension_families_together` (inside the dim peel) | two kinds of extension row must not pair |
| `MergeNode._extent_free_partials`, `join_resolution.extension_padded_addresses` / `_span_padded_addresses`, `get_join_type` | which NULLs here are absence, and whose? |
| `strategy_builder._cover_groups_for_mandatory` | which group holds a column's value on which rows? |
| `strategy_builder._fold_passthrough_parents` / `_drop_ancestor_parents` / `_fold_covered_contributors` | can this contributor go? (asked of COLUMNS; a contributor of ROWS is invisible to it) |
| `condition_placement._preserved_final_branch`, `_uncovered_exposing_output_contributor` | will a join re-admit rows this WHERE removed? |
| `optimizations/predicate_pushdown`, `join_upgrade`, `null_safe_join` | is this side null-extended, and is that NULL a value? |

They disagree at the edges, and every disagreement is a wrong answer or a lost filter. The derived-concept key-domain rule (a derived concept is NULL where its key's entity is absent) could not be implemented in one place because there is no place: the prototype needed five new predicates and three `keep=` parameters, each restating "which rows, which entities" locally, and each suite failure was one more component with a different implicit answer.

Both design docs already say the same thing from the other side: `domain_graph_design.md` and `v4_network_discovery_design.md` each have a "nullability is not modeled" section. `DomainGraph` relates VALUE SETS of concepts across the model. What is missing is its statement-level counterpart.

## The idea

A **keyspace**: computed once per plan, before grouping, from the requested keys.

- A **region** is one kind of row: a set of entity keys that are PRESENT on it. Customers/orders gives `{customer, order}` and `{customer}`.
- The statement's universe is a set of regions. They are disjoint by construction (the Venn diagram's cells, not its circles).
- A concept is **defined** on a region when every one of its FD-minimal keys is present there; elsewhere it is ABSENT, which renders as NULL but is not a NULL value.
- An aggregate is evaluated OVER regions (that is why `count(order_id) by customer_id` is 0, not NULL, on `{customer}`); a row-stream derivation is evaluated ON the regions where it is defined.

Shapes it has to get right, all pinned by tests today:

| model fact | regions |
|---|---|
| `orders.customer_id: ~customer_id`, customer key demanded | `{customer, order}`, `{customer}` |
| same, customer key NOT demanded (join axis only) | `{order}` (what `spans_demanded_by` decides today) |
| items bind `~user_id` and `~product_id` | `{item, order, user, product}`, `{user}`, `{product}`. There is no `{user, product}` cell: "families never cross-pair" is this, stated once |
| `returns` binds `~order_id, ~item_id` as its OWN grain, `lines` binds both complete | one region `{order, item}`. `_ret_order` is a NULL VALUE there, `is_returned` evaluates to false (tpc_ds q94) |
| `orders.customer_id: ?customer_id` | one region; the NULL key is a value on a real row |
| `by rollup (k)` | subtotal rows are a grouping product, not a region |
| WHERE null-rejects a concept absent on a region | that region is empty for this statement (pin-heal, as a query) |

The region test, as far as the prototype got it: an extension row of a `~` key comes from a source binding that key completely, and carries whatever that source binds beside it. So an entity is present on the region iff some such source binds its key. "The span does not FD-determine it" is NOT the test (it breaks q94).

## Where it sits

`concept_strategies_v4._build_from_graph` is already a three-stage pipeline: `build_concept_graph` -> `build_group_graph` -> `build_strategy_node`. The keyspace is a stage between the first two, handed to the other two and to FINAL assembly. `_build_from_graph` recurses (rowset bodies, WHERE-phase sub-plans), so a keyspace is per PLAN, not per statement: a sub-plan's requested keys include its grain, which is exactly what made a statement-level gate fire inside tpc_h q20's sub-plan in the prototype.

Inputs: requested keys (output grain, aggregate grains, WHERE row args), `DomainGraph` (bindings with `~`, FD edges, subset/equal/incomparable), the WHERE.

What it owns: regions, presence, and the questions in the table above, as queries.

What it does NOT own: join rendering, source search, aggregate argument inlining (a stage-3 rendering choice: an aggregate re-derives its BASIC arguments from the root instead of reading the BASIC's group, which is why padding must never happen inside one), and the optimizer. Those consume its answers.

## Migration (strangler; SQL stays byte-identical until phase 4)

Every eager structural change to root grouping has broken something (see the memory notes on the dim peel, the filter-only bridge, the merged unnest bridge). So the first three phases change no plan.

0. **Read discovery first.** `source_planning.py`, `network_search.py`, `network_build.py`, `network_obligations.py` were not read when this was written. The plan assumes the source search consumes partiality through `extent_free_spans` and stamps only; confirm or correct that before designing the data structure.
1. **Read-only analysis.** Build the keyspace, attach it to `BuildInfo`, render it in the debug trace. No consumer. Unit-test it directly against the shapes table above: this is the first time those rules have a spec that is not a SQL string.
2. **Agreement assertions.** Under a test-only flag, each scattered site asserts its answer equals the keyspace's: demanded spans, the elected owner, pin-heal's healed set, `_extent_free_partials`. Run the full suite. Each disagreement is either a keyspace bug or a latent planner bug; triage them before moving anything. This phase is the real test of the idea and it is cheap to abandon.
3. **Move the questions, keep the answers.** Replace each site's derivation with a keyspace query, one site per commit, `zquery<N>.log` blob-hash A/B proving no SQL moved. Pin-heal stops rewriting datasources. The election stops running last.
4. **Place derivations by region.** A row-stream derivation is computed on a stream covering only regions where it is defined, unless it is NULL-propagating there anyway. Regions declare their join keys up front, so `_compute_concept_sets` demands them as hidden columns. This is where the key-domain rule lands, for every shape at once; it is what the prototype could only do behind gates (one span, span key projected, deliverable WHERE).
5. **WHERE by region.** An atom is evaluated on the regions where its inputs are defined; a region it null-rejects is empty; a null-accepting atom is evaluated after the regions are united. Replaces `_preserved_final_branch` and its relatives.
6. **Rows versus columns in the builder.** A contributor carries a region contract; the three folds check it instead of each growing a `keep=`.
7. Delete what is left: the padding-provenance matrix, `_keep_extension_families_together`, the span plumbing on `BuildEnvironment`.

## Status

Phases 0-2 are built, phase 3 is done (five sites moved) and phase 4 has landed: the oracle owes nothing (2026-09-20 and 09-21, same branch, PR #702). Everything below this heading is a record of what they found; the sections above are the original plan, unedited.

### Phase 0: what the source search actually consumes

The plan's assumption was half right. The network search (`network_model`, `network_build`, `network_search`, `network_obligations`, `network_coalescing`, `network_topology`) reads NO span, NO `extent_free_spans`, NO `DomainGraph`, NO `NULLABLE`:

- `network_build._candidate` reads `datasource.partial_concepts` once and turns it into one `BindingStrength.FULL | PARTIAL` bit per binding. That bit is the whole vocabulary (it is also the entire Rust FFI surface for partiality). `non_partial_for` + `condition_implies` (`ConditionFit.IMPLIED_EXACT`) can promote a partial binding to full per request, and `network_coalescing.downgrade_axis_bindings` MINTS partiality for coalescing axes with no stamp behind it.
- Spans reach the source planner only ambiently: `source_planning` builds `MergeNode`s without passing `extent_free_spans`, and `MergeNode.__init__` defaults it to `environment.extent_free_spans`, which `build_strategy_node` sets per group. That side channel is what a phase 3 has to inventory.
- `plan_source` runs in stage 3, per group, after grouping. A pre-grouping keyspace is upstream of it and does not have to serve it.
- The search re-derives row-POPULATION facts (`_row_complete`, `_grain_classes`, `_drop_dominated_arms`) but never entity presence. The stronger partiality reasoning is in the legacy single-scan path (`select_helpers/source_scoring.py`, reached through `_direct_source`) and `select_node_v2.scan_stamps`, the only `NULLABLE` reader.
- There is no model-level index of address -> {complete binders, partial binders}; `SourceNetwork.full_binders` is per request and already query-conditional. The keyspace builds its own, cached per environment (`keyspace._model_facts`).

### Phase 1: the keyspace (`v4_helper/keyspace.py`, `models.Region` / `models.Keyspace`)

Built between `build_concept_graph` and `build_group_graph`, attached to `BuildInfo.keyspace`, logged when a plan has more than one region. No consumer. `tests/core/processing/test_keyspace.py` pins every row of the shapes table.

How regions are computed, since the plan left it open:

- **Entities.** A KEY is its own entity (its `keys` are an FK-path artifact: `customer_id` bound on `orders` lists `order_id`). Anything else resolves through its keys recursively, because an aggregate's keys are its `by`, which can be a property (`count(order_id) by status` is keyed on the order) or a ROLLUP grouping marker.
- **Regions are generated by sources.** A datasource row carries the keys it binds plus what a keyed lookup from them reaches (whole grain only, never through a source whose own grain is bound `~`). Each carried key remembers the `~` bindings on its path, its CAUSE; empty means the rows span the key's domain.
- A source whose carried keys cover a proper subset of the entities witnesses a smaller region. It survives only when every source carrying MORE reaches its identifying keys through a `~`; those keys are the region's `spans`. That one rule gives: the demanded/not-demanded split (a join-axis-only key is not an entity, so its source projects to nothing), q94 (`lines` and `returns` carry the same entities, so one region), `?` (complete binding, one region), and "families never cross-pair" (no source carries exactly `{user, product}`).
- An entity no source relates to a region's rows is cross-joined onto them, so it is present there.
- `emptied_by`: a WHERE null-rejecting a concept that is absent on a region empties it. This follows the RULE, so a derived concept counts (`status = 'delivered'`); pin-heal only trusts bound columns.

Not modelled: a lookup that may miss (a source whose grain is bound `~` and that binds a further key makes that entity OPTIONAL on the row: the same bug class from the other side), `complete where` partitions (as regions; site 5 does read `non_partial_for`, only to know that mutually exclusive slices are not each other's missing rows), and regions only a join of two facts can witness (the base region stands in).

Added by site 5, described there: `Region.witnesses` and `Region.completions`.

### Phase 2: the audit (`v4_helper/keyspace_audit.py`)

Inert unless `TRILOGY_KEYSPACE_AUDIT=<file>`; then each plan (sub-plans included) appends a JSON line per disagreement. Three checks:

- `demand` (deleted with the derivation it audited, once the election read the keyspace; `8d7b0d469`..`2fdbb632a` have it): the election's `demanded_extension_spans` against `Keyspace.output_demanded_spans`. Each election-only span also reported which sources the BUILT plan reads that bound it `~` (`read`); none meant the election's demand could not have changed anything.
- `heal` (deleted with the predicate it audited, once pin-heal read the keyspace: site 5): pin-heal must never heal a span whose region is live (`healed <= emptied`, over a keyspace replayed on the pre-heal datasources). Not equality: emptied is a fact about rows, healing also has to be safe for every other merge the dropped `~` would license, so pin-heal may decline (`test_anchor_needed_stays_partial`).
- `owner_pads`: BASIC / FILTER / WINDOW members of a span owner (or its descendants) that are absent on the span's region. Not a disagreement: it is phase 4's worklist.

Run it over the planner suites (`tests/core tests/discovery tests/engine tests/join_matrix tests/modeling tests/optimization tests/complex`, ~19 min; 3405 passed, every `zquery<N>.log` byte-identical).

#### What the first rounds changed in the keyspace

- **Completion spans.** The election also demands a span when no entity is absent: a source the plan NEEDS (it alone binds something requested) holds only some of a region's rows beside a source holding all of them (`returns` beside `lines`, a partial aggregate table beside its dimension). `Region.completes` records those `~` keys. "Needed" matters: TPC-DS `store_returns` binds `~item.sk, ~ticket_number` model-wide, and only a statement reading a returns measure is completed by it.
- **Demand is the election's question**, not "any output defined on the region": a span is demanded when some output is a function of what a keyed lookup from the span ALONE reaches (`Keyspace.span_reach`). TPC-H q20/q9 (`part.id` reaches no supplier: `partsupp` is many-to-many) and TPC-DS q05 (sale lines with no return are a region, but no output hangs off `item.sk`) separated the two.
- **A `~` survives a merge.** `merge a into b` respells the bound column onto `b` while `column_level_partial_addresses` keeps `a`; the binding is found through `origin_concept_address`. `partial_bridging._structural_partial` compares `column.concept.address` only, so pin-heal never heals a merged `~` key today (conservative, a lost optimization, not a wrong answer; gcat `payload.launch.launch_tag`).
- An existence-only node is a semijoin's subselect, not a row of the plan; spans are named by the address bound `~` (the spelling every other site uses), entities by the canonical spelling.

#### Closing the gaps (rounds 4-8)

Round 3 left 289 `demand` rows over 3405 tests: 224 where the election demanded a span and the built plan read NO source binding it `~` (`licensed_extension_spans` was model-wide: `select item.category, sum(price)` over `store_sales` "demanded" `item.sk` because `store_returns` exists), 29 where the WHERE empties the span's region, and 36 where a `~` binder WAS read and the keyspace saw nothing. Those 36 were keyspace bugs, fixed one kind at a time:

| what was missing | shape that found it |
|---|---|
| a `~` recorded under the merged-away address (`origin_concept_address`) | gcat `payload.launch.launch_tag` |
| a source with no declared grain: identified by its KEY columns less the ones that identify ANOTHER source (its foreign keys), so a lookup can enter it | TPC-H instantiated `customers` (adhoc07) |
| a property that identifies some source's rows is an entity | `region_dim` grain `(region)`, `fact` binding `~?region` (`test_join_upgrade`) |
| a key computed row by row is a function of its arguments' entity, not an entity | gcat `orbit_code` |
| a GENERATED key (`unnest`, constant) is the complete side of a `~` merged onto it | `merge orid into ~orid_2` (`test_const_equivalence_merge`) |
| a fan-out BRIDGE: no lookup leads from a launch to its engines (a vehicle has many stages), so no single source carries both; the partial source that joins the region to the rest keeps the unmatched members a region | gcat `test_should_group` |
| no complete source at all: the partial ones ARE the domain and complete each other | `web_orders` / `store_orders` (`test_dataset_merge`), enum unions. RETRACTED in step 2's third chunk: two partial bindings have no defined relationship |
| model components (sources sharing a column), not single rows, decide which entity is cross-joined onto a region | fell out of the bridge |

Tried and reverted: resolving a rowset key to the key it wraps. A rowset is a row SOURCE; `select even_orders.store_id` must not gain the stores no order references (`test_complex::test_rowset*` turned into keyspace-only demand).

Round 8: 3410 passed, SQL byte-identical, no keyspace-only span, no `heal` row. The 11 rows left where a `~` binder is read and the keyspace demands nothing, all election over-demand:

- **Grand totals** (2): `select order_count` from `agg_by_customer(~customer_id)`. No entity, one row, nothing to extend.
- **Rowset inner plans** (4: TPC-DS q64 x3, `test_aliased_outputs_keep_the_fk_axis`): the `~` source is read inside the rowset's OWN plan (`ss.is_returned` lives in `ss_rows_99` / `ss_rows_00`), which has its own keyspace and election; `extent_free_spans` is scoped per plan, so the outer demand never reached it.
- **`complete where` partitions** (1): `~city` is the partition discriminator, the union rule heals it, and no source holds a city no partition has.
- One test that truncates the source search on purpose.

Plus a whole-corpus SQL diff (a pytest plugin wrapping `BaseDialect.compile_statement`), election reading the keyspace vs not, over the 25 files holding every disagreement: 1028 statements, 2 differ. TPC-DS q29 (CTE renames and a reordered WHERE, 45 characters shorter: `extent_free_spans` leaves a CTE identifier once nothing suppresses a span there) and one gcat statement (two SELECT columns swap). Same rows.

### Phase 3, sites 1 and 2: DONE

`elect_extent_owners` and the dim peel's `_keep_extension_families_together` take the demanded spans as an argument, and `_build_from_graph` passes `Keyspace.output_demanded_spans`. `demanded_extension_spans`, `spans_demanded_by` and the `demand` audit are deleted; `span_members` stays (it is an FD question: what an extension row of a span carries). `zquery29.log` regenerated.

### Phase 3, sites 3 and 4: DONE (2026-09-21)

The two remaining model-wide scans of `column_level_partial_addresses` in join typing now read `Keyspace.in_play_spans` (every region's `spans | completes`, emptied regions INCLUDED: their rows are gone once the WHERE has run, and a merge below that point still sees their padding). `_build_from_graph` scopes it on `BuildEnvironment.in_play_spans`; each `MergeNode` captures it at construction, the way it captures `extent_free_spans` and for the same reason.

- **Site 3, the padding-provenance matrix** (`get_node_joins`, now `_span_padding_matrix`; `licensed_extension_spans` deleted).
- **Site 4, the host grain** (`MergeNode`, `host_stitch`): which `~` outputs make a side the host. Not on the original inventory; it had its own inline copy of the model-wide set.

Method: both answers computed side by side under the audit flag, the plan still built on the old one. 250 matrix computations and 166 host grains over the planner suites; 30 and 28 disagreed. What they were:

| what | shape that found it | outcome |
|---|---|---|
| a rowset body is its own plan with its own keyspace; the OUTER plan's merge reads padding made inside it | TPC-DS q64 (`ss_rows_99` / `ss_rows_00`), `test_semi_join_pushdown` | fixed: the matrix reads the spans of every merge below it (`merge_node.tree_in_play_spans`). On the node tree, not the environment, so a history-cached sub-plan still carries its spans. First place the unmodelled "rowset as a witness" is consumed. |
| a scoped join keys the pair on its canonical, not on the member bound `~` (`subset join pr.item.sk = ss.item.sk`; the keyspace names the span `pr.item.sk`). The model-wide set only matched because an unrelated `ss` source also binds `~ss.item.sk` | `test_q64_rowset_join_with_second_fact_join_hoist` | fixed: `_span_spellings` names a span through its `scoped_join_key_groups` group |
| model-wide over-approximation: an authored `union join`'s padding, or a plain dimension lookup, called `~` padding because some source the plan never reads binds the key `~` | q29 existence feeder x2, `test_join_hoist_inlined_dim_group_key_no_dangling_source` | the keyspace's answer stands |
| a composite-key join names `vehicle.variant` beside `vehicle.name`; only the requested key is an entity. Both sides shrink alike | gcat `test_full_join_issue_2` | the keyspace's answer stands |
| host grain `{ticket_number}` because `store_returns` exists in the model, in plans with no span in play | TPC-DS q34 / q73 / q79 / q64, the `complete where` city model | the keyspace's answer stands: it is what the code's own comment says ("with no licensed keys in play, grain coverage decides"). The host grain reads its OWN plan's spans, not the tree's: a rowset below is a row source, its extension rows are not this plan's to host. |

Then flipped with the audit still comparing: 3417 passed, every `zquery<N>.log` byte-identical, the residual rows exactly the explained ones above. A compiled-SQL diff over every file holding a disagreement (178 tests) moved nothing. (`gcat::test_environment` reorders two SELECT columns BETWEEN TWO IDENTICAL RUNS: pre-existing nondeterminism, and very likely the "one gcat statement" of the sites 1-2 diff. Compare distinct SQL per test, not by statement index: the benchmark tests compile a varying number of times.)

Already done by transitivity, no change needed: `MergeNode._extent_free_partials`, `extension_padded_addresses` and `_cover_groups_for_mandatory` read the election (`extent_free_spans`, `ownership.owner_of`), and the election has read the keyspace since site 1.

### Phase 3, site 5: pin-heal asks the keyspace which rows die (2026-09-21)

`partial_bridging._extension_killed` (a proven-non-null bound concept outside the key's FD closure) is replaced by `Keyspace.binding_is_complete(source, span)`: a source's `~` on a key costs the plan nothing when the WHERE empties every kind of row that source has no match for. Pin-heal keeps what is its own: the anchor guards (`_pair_anchors` / `_anchors_dispensable`: emptied is a fact about rows, healing must also be safe for every other merge the dropped `~` licenses), the model-component gate on killers, and the `then where` exclusion. The phase 2 `heal` audit and its replay plumbing are deleted with the predicate they audited.

What had to be learned first, in order:

- **It stays at the statement seam, and the reason is not the one first assumed.** Nothing between `get_query_node` and `build_keyspace` READS a `~`, but `generate_graph` captures the datasource OBJECTS, so a heal after it rewrites a mapping nobody reads (tried: per-plan healing inside `_build_from_graph`; 17 failures, wrong rows and `duplicate alias` binder errors from healed and unhealed scans of one source sharing an identifier). So the keyspace comes to the seam instead: `build_concept_graph` + `build_keyspace` need no graph, and run only once the cheap gates pass (a `~` column exists, the WHERE proves a bound column non-null).
- **Pin-heal was model-wide too.** It healed every `~` column in the model whose extension the WHERE would kill: 143 of 331 audited plans healed only spans the plan never has in play (TPC-H q1 "healed" `part.id` and `order.customer.id`). Restricting it to the statement keyspace's in-play spans moved no SQL over the 23 files where it fires (486 tests, 374 compiled-SQL sets), so those heals were inert and `binding_is_complete` is false out of play.
- **Completion emptiness was the keyspace gap** (5 tests: TPC-DS q83, `test_return_date_pin_heals_unified_returns`, three `returns`-beside-`lines` shapes). `where return_amount is not null` kills the lines no return matches, but no entity is absent there, so no region dies (q94's lesson, from the other side: a NULL value is still rejected). `Region.completions` now names each source holding only some of a region's rows, its `~` spans, and `emptied_by`: the null-rejected concepts only that source supplies.
- **Whose rows a region is** (`Region.witnesses`). Healing P's `~` forgets only the members P lacks, so a live region P itself witnesses (q83's saleless returns) does not block it; a region another source witnesses does.
- **`complete where` slices are not each other's missing rows.** With no complete source the partial ones complete each other (`web_orders` / `store_orders`), but not across mutually exclusive partitions (the unified returns per channel): the union machinery stacks those. First place the keyspace reads `non_partial_for`.
- **Bound columns only.** `emptied_by` follows the RULE, so `status = 'delivered'` empties `{customer}`; the rendered CASE still yields a value on those rows until phase 4. Pin-heal builds its keyspace with `null_rejected=` its bound-column proofs. With that the two judgments agree on every binding in the corpus, and the flip is byte-identical. Without it there was exactly one difference, a real improvement held back for phase 4: gcat `test_spacex_aggregates` heals the MERGED key (`merge first_org into ~org.code`, which `_structural_partial` could never see) under the derived `launch_date.year >= 2015`, `LEFT OUTER JOIN` to `INNER JOIN`, same 12 rows.

Method, again: both judgments computed side by side inside the anchor guards, the plan built on the old one, every difference triaged (6, then 2, then 0), then flipped. Known noise when diffing SQL: `gcat::test_environment` reorders SELECT columns between identical runs.

### Phase 4: derivations placed by region (2026-09-21)

The oracle's `OWED` list is empty. A live extension region whose rows would give an absent derivation a value gets a ROOT bucket of its own, the region's **domain** (`group_graph._add_region_domain_buckets`, id `grp:root:root:∅:extent:<spans>`, `GroupAttrs.extent_spans`). It is the prototype's plan (`docs/handoff_extension_row_semantics.md`), with every question it asked of FD and stamps now asked of the keyspace, and its three gates gone:

```
grp:root:root:∅                          (customer_id, delivery_date)  solid: orders rows only
grp:basic:d*:…                           (status)                      over the solid stream
grp:root:root:∅:extent:local.customer_id (customer_id[, name])         the region's rows: customers
FINAL                                    customers LEFT JOIN status-stream ON customer_id
```

| the prototype's gate | what replaced it |
|---|---|
| exactly one demanded span | one domain per live region, per sub-graph label. Two `~` families assemble as `solid FULL JOIN users FULL JOIN products`; regions are disjoint, so nothing cross-pairs (`test_composite_grain_families_with_by_span_aggregate`: `'LATER'` became `None` on both kinds of extension row) |
| the statement projects the span key | the region's spans ride as hidden columns: a secondary member of the domain and of every bucket it was split from, an output of whatever reaches FINAL (`region_join_keys` in `_compute_concept_sets`), and a hidden output of the FINAL merge, which hosts by the licensed keys it EMITS (`select name, status`) |
| every WHERE input deliverable, and projected | `emptied_by`: a dead region gets no domain. An atom over what the domain carries filters the domain. A null-accepting atom over something absent is hosted at FINAL (`PlacementReason.FINAL_SPAN_DOMAIN`, `_reads_past_region_domain`), and rides there as a hidden column when the statement does not project it (`select customer_id, status where amount is null`). What is left is phase 5's, see below |

What absence means, in one place: `Keyspace.defined_on(address, region)`. What an extension row holds: `Keyspace.carried_on` (keyed on what a lookup from the region's spans reaches; a cross-joined entity is present but not carried). `_null_on_padding` survives as a RENDERING gate only (plain arithmetic is NULL on padding anyway, so no domain and no moved SQL); it is why no TPC-DS / TPC-H `zquery<N>.log` moved.

What had to be learned, in order:

- **A BASIC is keyed on what it READS, not on its declared keys.** `coalesce(sum(amount), 0)` lists `order_id` in `keys` (the aggregate's argument), so the keyspace called it absent on `{customer}` and the statement-wide need test gave the ROLLUP trap a domain (`(3, 0)` became `(3, NULL)`). The prototype never saw this because its need test walked lineage from one bucket and stopped at the first aggregate. `keyspace._read_addresses`.
- **The need test is statement-wide, the split is per bucket.** The dim peel moves `qty` (what `line_status` reads) out of the bucket that holds `user_id`, so a walk from one bucket's members finds nothing. Every eligible ROOT bucket of the label contributes its carried members, including one the dim peel already keyed by the span (wholly carried: it IS the region's rows, and FINAL would otherwise read `name` off a solid sibling that passes it through).
- **A domain contributes ROWS.** The mandatory cover only sees columns, so `_add_region_domain_contributors` adds the domain as a contributor of no concepts (`select order_id, max(amount) by user_id`: the user with no order is a row of all NULLs). Not when a contributor already read it. Still phase 6's `keep=` plumbing in the three folds.
- **A domain stays whole at FINAL.** `_wrap_for_grain` buckets a root's concepts by natural grain; a two-span region came back as one GroupNode per key, each stitched on its own, NULLing the other key on the extension row.
- **What the domain carries is PARTIAL on the solid stream**, exactly like the span key: the names of customers WITH an order. `MergeNode._extent_free_partials` stamps it (`BuildEnvironment.extent_free_carried`, `ExtentOwnership.suppressed_carried_for`); without the stamp an aggregate `by name` made the domain look redundant and the merge dropped it (`select name, count(status)` lost `('cat', 0)`).
- **An aggregate is evaluated OVER a region when the region's rows hold its argument** (owner answer 4). `count(customer_id) by status` reads the domain beside the solid `status` stream (`_aggregates_over_region`, an edge domain -> aggregate group), counting the orderless customer under `status = NULL`; the domain is then not a FINAL contributor a second time.
- **The OPTIONAL entity is not unmodelled after all.** `returns` binding `~order_id, ~item_id` plus its own `return_id` is a larger SOURCE than `lines`, reached through a `~`: the keyspace already calls `{order, item}` a region with spans `{order_id, item_id}`, and the domain machinery places `coalesce(reason, 'none')` on the returns stream (`test_optional_entity_is_absent_on_rows_without_it`, with its own materialized twin).
- **Only a bucket that MIXES pads.** Padding happens where something the region carries is sourced beside something absent on it; a bucket holding only one kind is solid (or the dimension) by construction. Two over-reaches this removed: an order-keyed dim peel holding the fact's own `customer_id` FK was read as "the region's rows" (`select customer_id, order_id, order_rank` went keyless), and an order-keyed bucket with nothing carried got the OTHER family's span as a hidden member, fanning orders out by product.
- **A preserving join is part of a merge's identity** (`QueryDatasource._compute_identifier`, `_null_extended_sides`). Found by the fix above, latent on main: two merges of the same members whose joins are typed differently (one consumer's scan of `orders` projects the `~user_id` the other's does not) shared an identifier, `merge_ctes` folded them into one CTE, and both joins landed on one alias (`Ambiguous reference to table`). The signature names the NULL-EXTENDED side, not the join as written: `a LEFT JOIN b` and `b RIGHT JOIN a` are one relation, and keying on the written form split two copies of one merge that a FINAL fold relies on recognising (TPC-DS `test_website_demo` grew a self-join). CTE names are assigned in first-seen order, so nothing else is renamed.
- **Only a region with rows of its own gets a domain** (`Region.has_own_rows`: some complete source witnesses it). A region the keyspace keeps only for a COMPLETION (a partial aggregate table beside its fact) has no row where an entity is absent: every row of it is a row of the larger source. Found by order-dependence, not by a test of its own: a TPC-DS aggregates test persists a `partial datasource` at ticket grain into the session environment, every later statement over `store_sales` then has a `{ticket_number}` completion region, and a domain for it is the FACT sourced a second time, joined back to a customer-grain derivation on the customer key (`test_website_demo`: a fan-out that `limit 5` hid, and that the row diff caught).
- **Only a region the statement asks rows of** (`_region_is_demanded`): an output is a function of what its span reaches (`Keyspace.output_demanded_spans`, the election's question since phase 3), or an aggregate counts the region's rows. `select order_id, status where name = 'ann'` asks for orders; the first cut gave the customer region a domain anyway and FINAL added it back PAST the WHERE, one all-NULL row per unmatched member (gcat `test_merge_with_filter`: every organization but `PLAN`). The test asserts `len(rows) > 0` and runs through `execute_query`, so neither it nor the row capture saw it; the SQL diff did.
- **A rename of what the domain carries is rendered on the domain** (`item_desc as d`, `name as n2`): any other host holds it for the matched members only, and a rename canonicalizes to its source, so left visible on its old host it reads as a COMPLETE copy and the merge drops the domain as redundant. Two builder details went with it: the row-only domain is added AFTER `_promote_final_aliases_to_grouping_contributors`, which deletes a contributor left with no concepts, and `MergeNode`'s covering-parent shortcut now ignores hidden outputs the way its dedup already did.
- **The row the old plan got wrong in the other direction.** `count(grain(order_number, item_sk))` for an item no sale references was 1 (the hash of a padded row); it is 0 now (`test_unsold_item_counts_no_lines`).

How it was checked, since nothing here is byte-identical: the oracle, then a rows-and-SQL A/B over the planner suites against the same code with phase 4 switched off by a pytest plugin (`local_scripts/keyspace_ab/ks_nodomains.py`; no production switch). `sqlcmp.py` numbers CTE names by first appearance first, because the name pool is shared by a whole session and one more CTE early on renames every later statement. Three of the bugs above were found by that diff and by nothing else.

### Phase 5: WHERE by region (2026-09-23)

The audit's `where` worklist, run over every planner suite on the phase 4 state, was EMPTY: no statement in the suites had a region that needed a domain and got none. The members were all hand-built, and phase 4's give-up (`undelivered` in `_add_region_domain_buckets`) is now unreachable in the oracle's model. The rule, as built:

- **A value the region's rows carry is deliverable** (`_filters_region_domain`: `Keyspace.carried_on`). A domain member (`name`) filters the domain, as before. A carried value no domain column holds (`activity`, a scalar over an aggregate by the span) has its producer read the domain, the condition phase of the scope included (`_feed_region_domains_to_present_scalars` matches on scope, not label), and the atom is restated at FINAL over the united rows (`_reads_past_region_domain`, `PlacementReason.FINAL_SPAN_DOMAIN`). The constraint edges that would join the producer into the solid stream are dropped for that atom (`_detach_final_span_domain_producers`): otherwise the branch pairs on solid keys, the aggregate never sees the orderless customer, and the atom lands in its HAVING.
- **Only when the statement's final rows decide the atom** (`projection.decided_at_output_grain`): every output that crosses an aggregate is grouped at a grain determining the atom's input. `select customer_id, count(order_id) where activity = 'dormant'` restates fine; `select status, count(customer_id) where activity = 'dormant'` does not (the count is by `status`, the atom is per customer), and the region keeps the padded plan: an OWED oracle query, and the audit's `where` row. The A/B found this on gcat `test_date_filter`: a launch-day `BETWEEN` had moved above a per-month count, rows unchanged on that data, plan wrong.
- **Such a WHERE alone earns the region a domain** (`where_over_region`), with or without an absent derivation: the materialized twin, where `status` is a bound column, took the padded plan and lost the orderless customer to an INNER join on the order key.
- **An atom over the span key keeps the extension row.** `select customer_id, status where customer_id in (2, 3)` returned only `(2, 'delivered')` on both models: the merge read the atom as proof the fact side is present. Three narrowings now know a `~` key whose extension rows the plan returns (`Keyspace.output_demanded_spans`, on `BuildEnvironment.demanded_spans`, captured by every `MergeNode`) is a coalesced key: the merge's own WHERE (`_join_proofs`), a branch's filter over a key it was built not to extend (partial there whatever the domain exposes at FINAL), and the "filtered branch is the population" rule, which treats what a domain carries (`QueryDatasource.extent_free_carried`) like the span key.
- **A null-rejecting atom over an absent value empties the region** (`emptied_by`), unchanged from phase 4: no domain, the filter sits on the fact side, the region's rows are gone.

Three general planner bugs sat on the way, none of them about `~`, all silent on main, all fixed on this branch with `tests/engine/test_where_over_aggregate_by_key.py`:

- a WHERE over an aggregate by a key was DROPPED when the key and one of its properties were both projected (`select customer_id, name where n_orders > 1` returned every customer): the FINAL cover re-sourced the peeled dim bucket freshly and kept only the atoms the scan itself can state. The fresh re-source now only replaces the built node when it can restate every atom; a kept node unhides the join keys it sourced for its constraint parent, and its siblings are widened to carry them (a sibling reading a region domain vouches for the domain's spans as merge keys whatever grain it advertises).
- a BASIC adopted a parent grain key the parent HIDES and the CTE could not source it (`Missing local.order_id in ...`, the crash the plan listed as pre-existing). Hidden parent outputs are not rendered, so `outputs_with_parent_grain_keys` no longer offers them.
- with those two fixed, `select name, delivery_date where n_orders > 1` tripped the keyless-join guard instead: the kept dim node and the fact stream both hid the only axis. Covered by the unhide-and-widen rule above.

`_preserved_final_branch` and `_uncovered_exposing_output_contributor` are NOT retired: they fire on shapes without a region domain (a dimension filter LEFT-joined back to a qualifying aggregate), and nothing here replaces that. Retiring them is an A/B of its own.

Found on the way, recorded, not changed: `select undelivered_customer` alone now returns `('ann',), (None,)` on the derived model, where the plan's owner answer 5 expects the filtered set `('ann',)`; `select customer_id, undelivered_customer` already returns the plan's expected `(1,'ann'), (2,NULL), (3,NULL)` on the derived model as a side effect of phase 4, with no test pinning either. Both belong to owner answer 5's change (below). `_keep_extension_families_together` is NOT dead: raising inside its merge branch fails 12 tests across the processing, optimization and engine suites.

The branch's final A/B (2026-09-24), branch base `f195602c3` against `5552f4cec`, rows and SQL over the planner suites: 74 tests' SQL differ, 18 tests' rows. Every row move is `now()`, a worktree path, or an unordered `LIMIT` (SQL identical), except the two documented phase 4 changes (`'LATER'` -> `None` on extension rows in `test_duckdb_partial_key_assembly`). Every SQL move is a phase 4/5 shape (a region domain: `customers LEFT JOIN ... coalesce(...)`, the rename-on-domain rule, a `count(CASE ...)` where a `?` filter beside a pre-filtered scan used to push), a join written the other way round by `_null_extended_sides` (`a RIGHT JOIN b` -> `b LEFT JOIN a`, rows identical), or column/atom order. Nothing owed.

### Owner answer 5: a filter concept is a value (2026-09-24)

`filter X where COND` (`X ? COND`) in a SELECT is `X` where the predicate holds and NULL elsewhere; it narrows only itself. `tests/engine/test_filter_concept_is_a_value.py` pins the rows on a plain model (the oracle cannot judge this one: a bound column restricting the stream is the same bug), and the oracle's `HOLDS` gained the `~` spellings (`select customer_id, late_name`, `select name, late_name`, `select late_name`, ...).

- **The push is gone from every intermediate shape.** `gen_filter`'s two carve-outs (`pushable_siblings`, `siblings_constant`) are deleted; a filter group's predicate narrows its ROWS only when the statement shows nothing but filter values over that one predicate AND this group produces them (`_filter_intrinsic_pushdown_safe`, on `statement_filter_population`). A NULL row is then one nothing would keep, so `select late_name` alone stays the filtered set. The push is over the parents AS BUILT, never a statement WHERE: planned as a WHERE, the row atom of `customer_id ? count(order_id) by customer_id > 1 and product_name = 'Mouse'` lands under the count, which must see EVERY order (`test_filter_mixed_aggregate_row_predicate`). An intermediate filter (an aggregate's argument, TPC-DS q97's two `customer_item ? ...` beside each other) stays a per-row CASE, which is what the corpus baselines already were.
- **A property collapses to its grain.** `filter name where undelivered` beside `customer_id` is a property of the customer whose predicate reads order rows: the per-row CASE fans it out into `{name, NULL}`, one row per order. The FilterNode now groups to the outputs' entity grain (`_entity_grain`: a property stands for its keys, projected or not, so `select name, late_name` is at customer grain too) whenever a predicate input is not FD-determined by that grain (`build_fd_determines`, so `zip ? count(...) by zip > 10` beside the address key does not group), and the renderer's existing `CTE.filter_collapses_to_grain` MAX-collapses it. Not under an aggregate consumer (`collapse_to_grain`): `count(line_no ? commit_date < receipt_date)` counts the per-row CASE (TPC-H q4 grew a grouped self-join on `line_no` before that gate). The key spelling `customer_id ? ...` beside `customer_id` collapses the same way: `test_filter_with_optional_preserves_non_qualifying_rows` lost its `(None, 101)` fan-out rows, and `test_dim_attribute_peels_through_a_filter_alias` keeps EU-only store 2 with a NULL size; both were the narrowing the owner called a bug, both expectations rewritten with a note.
- **The population reaches the row universe, not the plan.** `statement_filter_population(mandatory_list)` is read by the keyspace (`_build_from_graph`: the region those rows are absent on is `emptied_by` it, so no domain pads them back) and by pin-heal (`get_query_node`). Without the second, `select undelivered_customer` on the `~` model returned `'cat'`: the emptied region still padded (the `~` merge is LEFT while the span is in play) and `delivery_date is null` is TRUE on the pad.
- **Pick-up item 4, done on the way: pin-heal trusts derived null-rejections.** `partial_bridging._proven_bound` used to keep bound columns only ("a derivation over an absent entity still renders a value until phase 4"); phase 4 is built, so a null-rejection over a derivation (`status = 'delivered'`, `undelivered`, `coalesce(amount, 0) is not null`) now stands for its entity keys (`Keyspace.keys_by_address`, what the derivation READS) and `binding_is_complete` decides; `build_keyspace`'s `null_rejected=` override is gone. `test_pin_heal_reads_bound_columns_only` became `test_pin_heal_reads_a_derived_null_rejection`, and the three keyspace tests that looked for an emptied region in the PLANNED keyspace of `where status = 'delivered'` now find no region at all (healed before the plan) and assert emptiness on the authored one. Corpus: TPC-DS q05 `FULL JOIN date_dim` -> `LEFT OUTER JOIN`, nothing else moved.

Owed from here: `select late_name, count(order_id) as n` is an `OWED` oracle query (the derived model returns the orderless customer as `(NULL, NULL)`, the zero-fill leftover; the materialized twin never demands the region, reading the filter's bound input). `select filtered_product_name having filtered_product_name is not null and manufact_matches > 0` (TPC-DS q41) renders the CASE plus the HAVING instead of the pushed WHERE, because the HAVING's aggregate `manufact_matches` rides the mandatory list as a hidden output, so the statement does not show only filter values; the HAVING's `is not null` removes the same rows the WHERE would have, so this is a plan choice, not rows (a HAVING probe over ANOTHER concept, `select late_name having name is not null`, still pushes and returns the filtered set).

### Review cleanups (2026-09-24)

A code-quality pass over the PR, all byte-identical SQL (full planner-suite `ks_sqldump` A/B: 3493 tests, only temp paths, a random temp-table hash and gcat `test_environment` differ):

- Region domains live in `v4_helper/region_domains.py` (bucket creation, feeding present scalars, detaching FINAL-restated producers). `GroupBucket.group_id` replaced `_group_id_for`.
- The four environment fields are one frozen `BuildEnvironment.span_scope: SpanScope` (`in_play`, `demanded`, `extent_free`, `extent_free_carried`), set/restored as a unit, captured whole by `MergeNode.span_scope`.
- A dim-peel group records `dim_keys` (bucket and attrs); nothing parses `dim:` / `:dim:` out of ids.
- The `keep=` id sets are gone: a region domain's node carries `StrategyNode.region_spans`, and `_fold_passthrough_parents` / `_drop_ancestor_parents` read it (the first use of phase 6's region contract). The id set was taken before `_apply_input_contracts`, so it could miss a replaced node.
- `gen_filter` reads entity keys from `keyspace.entity_keys` (its own `_entity_grain` is gone). `statement_filter_population` / `shared_filter_predicate` live in `projection.py` (no cycle import). `build_group_graph` / `elect_extent_owners` read demanded spans off the keyspace. `keyspace_audit.py` is deleted.
- Deliberately NOT changed: the keyspace is built twice per statement (pin-heal's, over pre-heal bindings and no materialized roots; the plan's, after). They answer different questions over different inputs; one keyspace needs heal to become a keyspace question instead of a datasource rewrite (phase 7).

### Phase 7 sizing experiment (2026-09-24)

Giving EVERY demanded live region with own rows a domain (dropping the `where_over_region or _evaluates_where_absent` gate in `add_region_domain_buckets`) fails 20 of the planner suites (`tests/core tests/discovery tests/engine tests/join_matrix tests/optimization tests/complex`), several as wrong rows. That is the worklist for retiring the election:

- `test_extent_ownership.py::test_every_span_routes_to_one_owner`, `::test_span_no_group_delivers_stays_unmanaged`; `test_keyspace.py::test_region_gets_no_domain_when_nothing_absent_takes_a_value` (these pin the gate itself; expected to change)
- `test_v4_root_d1_partial_keys.py::test_q20_d1_root_does_not_complete_part_key_from_dimension`
- `test_derived_key_domain.py`: materialization invariance for `select customer_id, status where name = 'cat'`, `select name, status where customer_id in (2, 3)`, `select name as n2, count(status) as n`, `select late_name, count(order_id) as n`; `test_rollup_subtotal_row_keeps_its_value`
- `test_duckdb_nullability_matrix.py`: `test_anchored_by_fact_key`, `test_agg_by_transitive_attr_no_license`, `test_licensed_transitive_dim_extends`
- `test_duckdb_partial_fk_field_report.py::test_field_report_select`; `test_duckdb_partial_key_assembly.py`: `test_all_keys_dims_and_metric`, `test_composite_grain_families_do_not_cross_pair[sum(qty) as total]`, `test_composite_grain_families_with_both_attributes`
- optimization: `test_full_join_lowering.py::test_suggested_null_reject_actually_unblocks_lowering`, `test_inline_broadcast_join_key.py::test_broadcast_join_key_survives_inline`, `test_no_invisible_contributor_joins.py::test_field_report_joins_nothing_it_does_not_read`, `test_semi_join_pushdown.py::test_already_filtered_tpch_aggregates_are_not_mirrored[query20]`

### Phases 6-7, step 1: every demanded region gets a domain (2026-09-25)

The sizing experiment's gate is gone: `add_region_domain_buckets` gives a domain to every demanded live region with own rows (not one whose spans an authored coalescing relation already unions: `union join ocust = cid` IS that key's domain, owner answer 2). The 20-test worklist is green; the oracle's `OWED` list is down to one (`select status, count(customer_id) where activity = 'dormant'`, the restate-above-a-coarser-aggregate shape). TPC-H q13 is `customer LEFT JOIN orders` again, 29 characters SHORTER than before phase 4. What it took, each found by a wrong answer or a lost row:

- **The domain's spans are the join axis everywhere.** `_final_merge_grain` counts a domain's `extent_spans` (its projection grain IS its spans), so a solid ROOT contributor preserves and joins on them instead of being shattered per natural grain (`select customer_id, status where name = 'cat'` on the twin: `LEFT JOIN cheerful ... INNER JOIN thoughtful ON order_id` killed the padded row); `_refresh_input_contracts` puts them in every parent's `preserve_keys`, so `_parent_nodes_for` never slices a domain below its spans (the address dimension stood in for the customer domain, ORPHAN and all); `_project_basic_aggregate_inputs` keeps them; FINAL emits the spans its parents READ, not only the ones a domain contributor carries.
- **Demand is the STATEMENT's, not the sub-plan's.** `_build_from_graph` hands the keyspace only the mandatory concepts in `environment.statement_output_addresses`: a condition feeder (`sum(...) by part.id, part.supplier.id`, TPC-H q20) lists its grain keys as outputs, and those are the axis it joins back on. The plan's own warning, finally built.
- **An aggregate is evaluated OVER the region** when a grouping key is carried (`count(order_id) by customer_id` is 0; `coalesce(sum(amount), 0) by rollup (customer_id)` is 0 on the customer with no order and 60 on the grand total) or its argument is (`count(customer_id) by status`, phase 4). Its named BASIC arguments are computed on the solid rows FIRST (`_project_basic_aggregate_inputs`, then the domain merges in): `count(status)` must not count a padded row. An INLINE argument has no node to compute on, so one that takes a value on padding (`sum(case when undelivered then 1 else 0 end)`, `count(grain(order_number, item_sk))`: the hash coalesces NULLs) keeps the aggregate solid, and the domain pads it at FINAL. Never when a row stream that must not see an extension row reads it (`_solid_groups`: `order_status <- case when qty = min(qty) by user_id ...` was 'LATER' on user 3). A ROLLUP aggregate declares no keys; `keyspace._entity_keys` reads its `by`.
- **A COUNT the domain pads is 0, said by the merge that pads it.** `QueryDatasource.zero_filled` -> `CTE.zero_filled`: the merge with one side holding a region's rows and one evaluated on the solid rows names the solid side's COUNT outputs, and the dialect coalesces exactly those (the old "nullable COUNT read through a join, unless the CTE groups" guess stays for materialized aggregates; a union-join arm's absent count is NOT 0, `test_composite_union_join_rowset_two_pass_aggregate_groups`).
- **Every row-stream derivation the region carries reads the domain**, not only a scalar over an aggregate by the span: `store_sk as d` beside a grand total, `name as n2`. `_add_region_domain_contributors`' rename rule is now unreachable in the suites (kept, to be deleted with the assembly-side ownership plumbing).
- **Rows versus columns, as a contract on the node.** `nodes.base_node.region_reads(node)`: the domains under a node, the spans whose extension rows are rows of its stream. Two contributors stand in for each other's columns only when they read the same regions: `_fold_passthrough_parents` folded `status` into the `n2` node whose merge read the domain, and the CASE evaluated on the padded row; `_drop_ancestor_parents`, `_fold_covered_contributors` and `_parent_nodes_for`'s covered-by-descendant test (the window sibling "covered" the domain: `count(order_seq)` lost customer 3) read it too. `_cover_groups_for_mandatory` hands a carried member to the domain only when nothing downstream READ the domain (else the domain rejoined at FINAL and RIGHT-joined the aggregate's guest group away).
- **The contract reaches join typing through the resolved datasource.** `StrategyNode.region_spans` is set when the domain is built (not per consumer: `_parent_nodes_for` ran before the per-consumer setting existed and dropped the domain as "covered" by the window sibling; and it survives `copy()`), `StrategyNode.resolve` stamps `QueryDatasource.region_spans = region_reads(node)`. `get_join_type` reads it PER PAIR (`region_holders`): the side holding the region joined on its span is preserved (the aggregate that read the domain, grouped by `address.sk`, out-hosted nothing under the binding rule because it did not EMIT `customer.sk`); the feeder is preserved too where its key holds a value NULL (`extent_nullables`, now chained through lookups keyed on a padded column: the guest order's address), and FULL when the merge holds a second family (the rows already joined carry its extension rows, NULL on this key: `test_dims_without_fact_anchor` lost `(None, 'C')` to a RIGHT JOIN); two holders of one region pair plainly, host inference stood down. `grain_utility._is_filter_population` never calls a side the population against a partner holding the region (a plain `orders` scan carries no `extent_free_spans` stamp, and `where customer_id = 3` INNER-joined customer 3 away). Every merge with a domain parent is a `host_stitch`.
- **The solid stream reads a span from the fact's own column.** A group built not to extend a span (its region domain completes it) has that span promoted to a full binding in the network search (`network_build._candidate`), skipped by `_complete_partial_requested`, and read as full by the legacy single-scan renderer (`create_pruned_concept_graph` / `resolve_subgraphs`, `excluding=`), which otherwise completed `orders.customer.id` against `customers` per concept. This is pick-up item 7's "correct and wasteful" join, gone: q13 658 -> 629 chars. And a group that reads no domain does not EMIT a carried member to a consumer that does (`_compute_concept_sets`: FINAL, an aggregate over the region), or the scan joins the dimension for `user.state` the domain delivers (thelook q09 is `users LEFT JOIN order_items GROUP BY` again).
- **Not modelled: a MATERIALIZED aggregate beside a region.** thelook `sales_agg` (`revenue` stored at `(~user_id, ~product_id)`, read at `(state, brand)`) is a rollup over the region's rows that the group graph holds as a ROOT; with the hidden spans in the request the network search's rollup target grain is the summary's own and `revenue` is `unreachable`. Such a bucket seeds no domain and keeps the padded plan. Found because the group's build failed and FINAL silently projected the outputs it had: a mandatory output no contributor renders now raises.
- **The FINAL-side patches this replaces so far:** the `keep=` id sets (review pass). The dim-peel `dim_keys` exposure at FINAL is scoped to plans with a domain (exposing them everywhere changed a plain TPC-DS plan's shape, `test_v4_root_condition_source_inherits`).

Verified with the worklist plus the guards (`tests/engine/test_duckdb_*`, `test_keyspace.py`, `test_extent_ownership.py` rewritten for one ownership: each span is owned by its region's domain, `test_span_reached_only_through_joins_is_owned_by_its_domain`), TPC-H, thelook rows, `test_partial_key_assembly_shapes`, and the planner-suite rows-and-SQL A/B against `6400a24fd` (below).

The step's A/B (rows and SQL over the planner suites, `6400a24fd` vs `d003ad771`): 3507 passed; 96 tests' SQL differ, 15 tests' rows. Every row move is `now()`, an unordered `LIMIT` (the five `test_non_benchmark_queries` cases, SQL moved, sample rows shuffled) or the two OWED queries that now hold. Every SQL move is a domain shape (the domain reaches the aggregate's input or FINAL; a solid CTE beside it), the promoted span (a `~` key read off the fact, a dimension join gone), or a join written the other way round. `zquery06.log`, `zquery07.log` (thelook) and `zquery64.log` (TPC-DS) moved within budget and are committed.

**Open at the end of step 1: seven SQL-size budgets** (thelook q08 580->1011, q16 625->1049, q19 1868->1941, q20 579->894, q21 703->1002, q22 740->872; TPC-DS q77 9153->11466; rows identical). The shape: the solid stream (`order_items INNER JOIN products` for `cost`) is a CTE beside the domains where the old plan was one flat `users LEFT JOIN order_items FULL JOIN products`. The lever is flattening a plain solid `MergeNode` (no conditions, no group) into the domain merge and sharing one `products` scan between the solid side and its domain; q16 is the materialized-aggregate shape above, re-planned as a real aggregate in a sub-plan with its own domain. TPC-DS q77 is the OPTIONAL-entity shape (`web_returns` bound `~item_sk, ~order_number` with its own return key): `sum(return_amount) by web_page` reads the returns domain beside a `sales LEFT JOIN returns` solid stream that contributes nothing it needs; an aggregate whose every argument the region carries should read the domain ALONE (`_parent_nodes_for` drops a parent only when a lineage descendant or grouping sibling covers it, never a domain that holds its rows).

### Phases 6-7, step 2, first chunk: the solid stream reads the domain for a NULL-propagating derivation (2026-09-25)

Four of the seven budgets (thelook q08 580, q19 1868 -> 1777, q20 579, q21 703: q08/q20/q21 byte-identical to their pre-phase-4 plans, `users LEFT JOIN order_items FULL JOIN products` again) fell to one rule, the last reader of `_null_on_padding` ("a rendering gate only" since phase 4):

- **A row-stream derivation that reads what a domain carries reads the domain of every region it is NULL on the padding of** (`feed_region_domains_to_present_scalars`). `item_margin <- sale_price - product.cost` reads `cost`, carried by the product region, and is NULL on a padded row however it is planned (`null_on_padding`, now in `extent_ownership.py`); evaluated over the padded rows it is NULL exactly where the rule says, it holds the same regions as its consumer's other contributors, and the solid root no longer re-sources `products` for `cost` beside the domain's own scan. Both halves were found by a wrong shape: without "reads something carried", TPC-DS q77's rename `wr_wp_id <- return_web_page.sk` (trivially NULL on padding) read the returns domain and padded its stream for nothing (11.5k -> 14.3k chars); without "every region", q08's basic read the product domain alone and FINAL stitched `users FULL JOIN <cte>`. `_solid_groups` seeds are now the derivations that TAKE A VALUE on the padding (`_takes_a_value_on_padding`), not every absent one, and it takes the environment. `_evaluates_where_absent`, dead since step 1, is deleted.
- **Latent, found by the first guard run:** `QueryDatasource.__add__` (the same-identifier and by-shape fold in `MergeNode._resolve`) built the sum without `extent_free_spans` / `extent_free_carried` / `zero_filled` / `region_spans`, so a folded child lost its `_extent_free_` identifier suffix while the carried joins still named the original (`Could not find CTE for datasource ..._extent_free_..._preserving_...`, `test_forked_with_state_and_brand`). The LHS's spans win: it is the key the fold happened under.
- **Latent, found by the oracle:** `null_padded_nodes` named only `join.left_cte` and the ON-clause sources as the padded side of a RIGHT/FULL join, never the FROM base and the sides joined before it, so a null-ACCEPTING atom (`amount is null`) was pushed below a RIGHT JOIN into the fact scan, where the padded rows then satisfied it (the materialized twin of `select customer_id, status where amount is null` moved from `(3, NULL)` to every customer). It now walks the accumulated left; `_push_having_into_group_parent` reads the same gate (`_predicate_safe_past_null_extension`: a null-rejecting HAVING still pushes past a FULL).

**Tried and parked (scratchpad, not committed): flattening the solid MergeNode into the domain merge**, the lever the end of step 1 named for q22 (`select order.status, user.id, revenue, margin`: the user region alone is demanded, the solid root is `order_items INNER orders INNER products`, and it renders as a CTE the `users` domain joins). `_flatten_solid_merges` (a plain merge beside a holder replaced by its parents, in `_pre_merge_parents` and `_assemble_final_node`) plus a merge-level contract (`MergeNode._region_scope`: a span some side HOLDS is extent-free for every other pair; a span in play the statement never demands licenses no extension rows) made q22 byte-identical to its old plan (`users LEFT JOIN order_items LEFT JOIN orders LEFT JOIN products`) and shrank q77 to 10847. It is parked because a flat chain needs three passes to understand a PADDED ACCUMULATED STREAM, and none does:
- `resolve_join_order_v2` pivots by fewest datasources, so a holder's RIGHT/FULL join can land before a feeder's INNER one, and the INNER drops the rows the domain just added (`test_composite_grain_families_with_both_attributes` lost `(None, None, 'C', None, None)`). Scoring holders last is worse: `users` leaves the FROM base and the next pass makes `orders` a RIGHT join (q22 188 -> 169 rows).
- `ensure_content_preservation` tracks only a prior join's ON-clause lefts, not the stream it padded (`has_prior_right` then preserves the new relation, the wrong side); the FULL -> RIGHT narrowing ignores the prior padding the same way.
- `held` handed to `get_node_joins` as `extent_free_spans` strips the HOLDER's own nullable marks (`extension_padded_addresses`), so a side that read three domains INNER-joined each of them at FINAL (`test_three_key_span_with_attrs`); the fix (a span a side itself holds is not extent-free for its marks) is in the parked diff.
- `in_play - demanded` as "unlicensed" over-reaches where the keyspace never marks a region demanded: gcat `select vehicle.class, launch_count` (a composite-key `~` binding, `lv_info` x `launch_info`) expects `LEFT OUTER JOIN launch_info` and got INNER.
Resuming it means teaching those passes the accumulated stream (or keeping the solid CTE and accepting q22's budget). One more thing the parked contract exposed, kept: `_push_having_into_group_parent` must stay blocked until the final join upgrade (pass 13's note); letting a null-rejecting HAVING push early past a FULL-joined group materializes the aggregate in its own CTE beside a retained consumer copy that nothing strips (usa_names `test_filter_constant_with_constant` 717 -> 1212; with the block, 774, the corrected padding refusing one strip). Tooling: `dbg.py` grew `file:<path>` queries and `--noflatten` / `--nofeed` / `--nocontract` / `--noheld` / `--nounlicensed`; note `--nofeed` is not a faithful revert (`_has_absent_inline_argument` reads `null_on_padding` too) and `env:` executors are unseeded, so rows come from pytest. The oracle's `dormant` OWED query HELD under the parked contract and reverts to xfail without it.

The chunk's A/B (rows and SQL over the planner suites, step 1's `d003ad771` dumps vs the committed state, the last run taken with the early HAVING push still in): 3511 passed; 35 tests' SQL differ, 13 tests' rows, every row move a `now()`, a temp path or an unordered `LIMIT` with identical SQL. The SQL moves are the thelook and forked-model region shapes (a solid root no longer re-sourcing a dimension the domain scans; `test_forked_full_column_set` 4267 -> 4711 is the same shape with every column), TPC-DS q04's null-rejecting HAVING (back to its baseline once the early push was blocked again), and the usa_names refusal above.

### Phases 6-7, step 2, second chunk: a BETWEEN empties a region (2026-09-25)

TPC-DS q77 (11466 -> 9162, budget 9336, pre-phase-4 baseline 9153) was not the lever the end of step 1 named. `wr_grouped` is `where return_date.date between ... select return_web_page.sk, sum(return_amount), sum(return_net_loss)`: the sales with no return are a region (`{item, order} ~[item.sk, order_number]`, `return_amount` keyed on the spans so "carried" there, `return_date` absent), and the BETWEEN on `return_date.date` rejects every row of it. The keyspace's `null_rejected` asked `condition_proves_non_null`, which proves comparisons and `IS NOT NULL` but not BETWEEN (that is `gather_non_null_proofs`' addition), so the region stayed live, got a domain, and the aggregate read `web_sales LEFT JOIN web_returns` beside `web_returns INNER JOIN date_dim`, RIGHT-joined on the spans. `null_rejected` now uses `gather_non_null_proofs`: the region is `EMPTY by [return_date.date]`, no domain, and (the same function is pin-heal's `proven`) `binding_is_complete("web_returns", item.sk)` holds, so the standalone body heals to `web_returns INNER JOIN date_dim GROUP BY` alone (1434 -> 470 chars). Inside the full statement the arm keeps the old `web_sales INNER JOIN web_returns` scan: heal's anchor guard reads `statement_authored_addresses`, the WHOLE statement's references (`ws.ext_sales_price` from the sibling `ws_grouped`, reachable from `web_returns` only through the `web_sales` anchor), so the rowset body's own keyspace is never asked. That is pick-up item 4's "one keyspace per plan", not this chunk's.

The "aggregate whose every argument the region carries reads the domain ALONE" lever is still unbuilt and, on this evidence, unneeded: q77 was a keyspace question (is the region live?) mistaken for an assembly one.

The chunk's A/B (rows and SQL over the planner suites): 3512 passed, the only failures q16, q22 and the three `ks_rowdump` cursor tests; every row move a `now()`, a temp path or an unordered `LIMIT`. Against a TRUE HEAD baseline (the change reverted in place, the 14 movers re-dumped in one session order) only q77 moves; the other moves the first-chunk dumps showed (`test_year_over_year_growth_not_recorrelated_on_names`, `test_multileg_*`, q04, usa_names 1135 -> 717) are between `new3.jsonl` and `d3905070c` itself: those dumps were taken with the early HAVING push still in. Guard: `test_where_between_on_an_absent_concept_empties_the_region`.

### Phases 6-7, step 2, third chunk: two partial bindings have no relationship (owner, 2026-09-25)

thelook q16 (1049 -> 565, below its pre-phase-4 625: no coalesces) was neither the materialized-aggregate re-plan step 1 proposed nor a rollup-modelling gap. The owner's rule: **there is no defined relationship between two `~` bindings on a key. The full set is always a complete source (or a `complete where` slice the union machinery stacks), and that is what a heal completes against.** Two consequences, each a deletion:

- **`_completions`' "no complete source: the partial ones ARE the domain, each holding members the others lack" branch is gone** (`web_orders`/`store_orders` as shards was an assumption the model never declared). With no complete source the key is not in play and nothing heals; `select order_id, web_id where web_id is not null` on such a model is now `UnresolvableQueryException: no complete sources` at planning, the model's own error, instead of a plan built on the shard guess. `_disjoint_partitions` and `_SourceFacts.partition` went with it. Tests: `test_partial_sources_with_no_complete_one_do_not_complete_each_other`, `..._never_heal`.
- **A sibling itself `~` on the key is not a pin-heal anchor** (`partial_bridging._pair_anchors`). q16 was a HEAL ARTIFACT: `user_product_sales` (`~user.id, ~product.id` at pair grain) anchored `order_items`' bindings through `_anchors_dispensable` (it carries both killers), so the `IS NOT NULL`s healed the rollup but not the fact; the planning keyspace, built from the mutated model, then read the healed rollup as a complete dimension of `(user, product)` pairs that `order_items` is partial against, a region with a domain, FULL-joined to the solid fact. With no partial anchor both heal and the plan is `users INNER JOIN order_items INNER JOIN products`. The `test_join_upgrade.py` fixture that blocked heal with a `~region` sibling now binds `region` complete (still an anchor, still carries the killer). Found by dumping `_model_facts` per keyspace build (`dbg.py --facts`): the second build showed the rollup non-partial. An "aggregation level" rule in `_witnesses` (a source whose grain is a coarsening of a finer source's keys witnesses nothing) was tried first and was WRONG on the same dump: after heal it absorbed `users` and `orders` too. Discovery does not need to know what a rollup is; the network search picks sources by aggregation level already.

**q22 rebaselined at 872 (owner, 2026-09-25):** the solid stream (`order_items INNER JOIN orders INNER JOIN products`) as its own CTE beside the user domain is the design's shape, and the parked flatten's failure modes were wrong rows; it stays parked as a cosmetic optimization.

The chunk's A/B (rows and SQL over the planner suites, against this session's `cf3ae047d` dumps): 3514 passed, the only failures q22's budget (before the rebaseline) and the three `ks_rowdump` cursor tests; the only SQL move is q16; every row move a `now()`, a temp path or an unordered `LIMIT` with identical SQL. No plan anywhere in the suites depended on the shard branch or on a partial anchor.

### Phases 6-7, step 2, fourth chunk: the retirement list, tried (2026-09-25)

Item 6's list was optimistic: most of it is the fallback for the regions the keyspace does not model yet, and every entry was tried by deletion. Method: a SENTINEL set (the keyspace guards plus gcat `test_case_key`, TPC-DS q64, the field report and thelook, ~15 s with `ks_sqldump`) reproduces the full capture exactly for the tests it holds, so each deletion was triaged in a minute and only the survivors went to the 16-minute A/B.

Live, restored, each with the shape that needs it written at the site:
- **`elect_extent_owners`' ranking branch**: a demanded span whose region got no domain. Unmanaged, every branch pads: gcat `test_case_key` (a composite-key `~` region the keyspace never marks demanded) read its aggregate off the padded `lv_info LEFT JOIN launch_info` root through three CTEs (2372 -> 2638); TPC-DS q64 scanned `store_sales FULL JOIN store_returns` where it scanned `store_returns` alone. Rows identical, plans worse. It is the fallback until "regions only a join of two facts can witness" and the composite-key demand are modelled.
- **`_keep_extension_families_together`**: the merged peel cluster is the bucket `add_region_domain_buckets` finds a region's solid source in; split, the user family mixes nothing and gets no domain (`test_field_report_select`, five assembly tests, wrong rows).
- **`_add_region_domain_contributors`' rename rule**: reachable after all. An alias rides its source's ROOT bucket, so `feed_region_domains_to_present_scalars` never sees it as a group (`customer_id as c2` in the oracle, `item_desc as d` in `test_unsold_item_counts_no_lines`).
- **host/`host_stitch` inference in `generate_joins`**: without it a domain merge FULL-joins its readers with coalesced keys (`test_materialization_invariance`, gcat).
- **the zero-fill guess in `dialect/base.py`**: the no-domain fallback's `coalesce(launch_count, 0)` (gcat; the test's `LIMIT 1` hides the NULL).
- **`_extent_free_partials` / `extent_free_carried`**: `test_unsold_item_counts_no_lines[select]` fails without them.
- **`_span_padding_matrix` / `_pads_for_different_members`**: silent on the sentinel, caught by the A/B. Without it a FULL join between two families' padding pairs NULL with NULL null-safely (`=` -> `is not distinct from`, gcat `test_full_join_issue_2`, `test_aliased_outputs_keep_the_fk_axis` with an extra `state_sk` key): the invented-row shape, which those tests' data does not show. `region_spans` does not yet answer "two readers of different regions".

Deleted: the region-spans sibling-grain widening in `_assemble_final_node` (`test_forked_full_column_set` 4711 -> 4408: the product-family side stops carrying `order_id`/`user_id` as extra FULL-join keys, rows unchanged), `kept_axis` and its unhide block there (no change anywhere), and the hidden-output withholding in `MergeNode`'s collapse shortcut (`test_key_beside_its_property_keeps_the_aggregate_where` 2938 -> 2215: a covering parent short-circuits the merge again, two redundant CTE layers gone). A/B (rows and SQL over the planner suites, against `e2deafacb`'s capture): 3515 passed, the three `ks_rowdump` cursor tests the only failures; the SQL moves are exactly those two tests (plus faa's LLM-authored query); every row move a `now()`, a temp path or an unordered `LIMIT`.

### A rowset as a witness (2026-09-26)

Pick-up item 5, the placement half. `test_unsold_item_counts_no_lines[ROWSET_QUERY]` is promoted: `select s.d, count(grain(s.o, s.sk))` over `rowset s <- select order_number as o, item_sk as sk, item_desc as d, quantity as q` returns `('gamma', 0, 0)` for the item no sale references, as the direct spelling does. What was wrong: the body padded the item (its own keyspace, `{_s_o, _s_sk} | {_s_sk} ~[item_sk]`), the boundary marked `s.o` nullable from that LEFT join, and the OUTER keyspace had one base region (`{s.o, s.sk}`: no datasource binds a handle, so `_witnesses` found nothing), so the hash was read as solid and counted the padding row.

- **A rowset the plan reads is a source of its keyspace** (`keyspace.RowsetWitness`, `_rowset_sources`). Its rows are the body's live regions, respelled in the handles: one `_SourceFacts` per body region, grain the key handles present on it, bound every handle defined on it, and a handle bound `~` when a smaller region of the same body spells its key as a span (the larger rows reach the smaller region's rows through it, as `sales` reaches the items through `~item_sk`). The outer keyspace is then `{s.o, s.sk} | {s.sk} ~[s.sk]`: the same two regions the body had, in the reader's spelling. A body region whose span no handle spells is skipped (the reader cannot name it; its rows read as the base region's, as before). The body's keyspace is computed the way the body's own plan computes it (`rowset_witness.py`: `build_nested_select`, healed, its WHERE and filter population, `build_concept_graph` for the declared attrs) and cached on the history per rowset; a multiselect body gets no regions. `statement_keyspace` is the one seam every plan's keyspace is built through now (the population, the statement-output filter, the witnesses, the heal audit).
- **A rowset bucket is split for a region like a ROOT bucket, but only when something must be evaluated on the solid rows** (`_needs_solid_rows`: a row-stream derivation that takes a value on the padding, or an aggregate whose inline argument does). The boundary already holds the region's rows (its body padded them), so a plain read of a padded rowset keeps its one boundary; step 1's "every demanded region gets a domain" is for buckets that pad by being read. The domain is a ROWSET bucket (`grp:rowset:d0:∅:rowset:s:extent:s.sk`, the boundary again, projected to what the region carries) and the solid side is the same rowset bucket, elected extent-free for the span.
- **The solid side is the body planned with the region owned above** (`SpanScope.owned`, `V4History.owned_spans`, part of the build key; `RowsetWitness.body_spans_of` maps the reader's spans to the body's). The body keeps its region (a window or aggregate the body computes OVER the region must still read the domain) but no group of it extends the span, FINAL included (`extent_free` unions `owned` at both sites in `build_strategy_node`), and a merge's region holders lose the spans it is extent-free for (`get_node_joins`): the body's own domain no longer preserves at its FINAL, the promoted binding makes the join INNER, and the restricted boundary is `items INNER JOIN sales`. Every plan under the body inherits the restriction, as it inherits heal; a construct nested inside starts over. Not dropping the region from the body's keyspace was the design point: the first cut did, and it would have computed `rank sk by d` over the solid items.
- **The restricted boundary stamps what the region carries partial.** `_extent_free_partials` lives on a solid group's MergeNode; a rowset boundary has no merge, so the domain boundary was `deduplicate_nodes`' "extraneous parent" (its `{s.sk, s.d}` a subset of the solid boundary's, all complete) and the aggregate lost it. `resolve_rowset` marks the owned spans' handles, and the handles `extent_free_carried` names, partial.
- **Two latent bugs on the way.** `_elide_single_parent_passthrough` collapsed a single-parent projection into a copy of its parent and dropped the projection's `region_spans` (a RowsetNode is a SelectNode with one parent; a ROOT domain never is). The keyspace tests' seam moved with `build_keyspace` (`rowset_witness.build_keyspace`).

Plan shape, second pass (rows right throughout; the first cut had three FULLs where the direct spelling has one LEFT, and the trace of every `get_join_type` answering FULL found five causes, four fixed):

- the domain boundary was widened with the body's grain key `s.o` by `_carry_join_keys` (the aggregate's input contract names `(s.o, s.sk)`), so the aggregate joined the domain on both and the domain was one row per SALE LINE. A rowset domain is now a `force_group` GroupNode over the boundary, to its members (the boundary's grain claim over a projected subset of handles is an overclaim: its rows are the body's), and `_carry_join_keys` never widens a rowset domain (a ROOT domain is a dimension scan and carries a key it binds: `test_licensed_transitive_attr_span` broke when the guard covered both);
- the aggregate's solid pre-merge (`questionable FULL JOIN wakeful`) was built under the aggregate group's scope, which is PERMITTED the span because it reads the domain, so the two projections of one boundary were typed as two partial sides. The solid merge is built extent-free for the domains' spans, and `partial_binding_sources` names a boundary as its own binding when no leaf `~` stands behind the partial (two projections of it are one binding): INNER;
- `region_reads` walked below the boundary into the body, so the outer merge saw the body's `local.item_sk` beside the reader's `s.sk` as two families and went FULL. `region_reads` stops at a boundary (`StrategyNode.region_boundary`); the domain's GroupNode carries the reader's spelling. Tried and reverted: stamping every boundary with its witness spans (an unsplit boundary IS the region holder) grew `test_rollup_label_over_union_joined_rowsets` by a dedup layer (2546 -> 2948); the unsplit boundary stays an ordinary node, as before.

A/B of this pass (compiled SQL over the planner suites against the first landing, 3632 tests): the two rowset tests, `test_forked_full_column_set` (4408 -> 4342, `FULL JOIN` -> `INNER`/`LEFT` inside the aggregate's solid pre-merge, rows asserted) and `test_forked_with_status` (CTE order), plus the BigQuery merged-key fixture, whose coalesced key is no longer a FULL join's ON key (the same tightening); `test_complete_merged_key_keeps_the_bare_equality` re-anchored on what the fixture still renders, the ON-key rule itself is `test_null_wrapper_encodes_only_illegal_full_join_keys`. Everything else byte-identical (temp paths and gcat `test_environment` aside).

Plan shape, third pass (the two leftover FULLs, `domain FULL JOIN solid-stream ON s.sk` and FINAL): both were plan-time typings the optimizer was left to narrow, and three rules stood behind them, not one.

- The restricted body's merge was typed RIGHT OUTER by the extent-free rule (anchor the fact), and `find_nullable_concepts` then stamped every column of the accumulated left (`items`) nullable; the rowset handles inherited that as VALUE nulls (`nulls_are_values` finds no upstream carrier for a handle and stays conservative), so `s.o, s.sk, s.q, s.d` were all value-nullable on the solid stream. An extent-free span is now INNER at plan time when the other side carries the span's whole domain (`complete_key_domain`: an unfiltered, unlimited scan binding it complete, or a read keeping every row of one through `preserved_sources`) and the binder has no NULL on it: the plan-time half of the optimizer's `_pair_side_fully_matches`.
- The holder rule's feeder-values veto read every connecting key. `s.d` (`item_desc string?`) is a genuine value NULL on the solid reader, but the domain carries it value-nullable too and the pair is null-safe. `_unpaired_value_nulls`: a value NULL on the region's own key always vetoes (a guest order names no member); on another key only without a value-null partner on the holder, which is exactly what `get_modifiers` pairs.
- The host rule had the same veto, and the rowset spelling's FINAL was the direct spelling's own plan-time FULL (host `young`, feeder the aggregate partial on `s.d`), which the optimizer narrows for the direct spelling and not for a ROWSET handle (`_genuine_partial_stamp` discounts the stamp). Same helper.

The rowset plan is now `items INNER JOIN sales` (body), `domain LEFT JOIN solid ON s.sk`, `young LEFT JOIN cooperative ON s.d`; pinned by `test_unsold_item_counts_no_lines` (no FULL in either spelling) and the three `get_join_type` cases plus `complete_key_domain` in `test_join_padding_provenance.py`.

A/B (compiled SQL over the planner suites, 3679 tests, `350169565` vs this pass): 15 tests differ. Noise: the four `test_duckdb_file_source` temp paths, gcat `test_environment`, faa `test_llm_execution` (an OpenAI-generated query). The four rowset tests (rows asserted). `test_v4_parity[group_to_by_dimension]` and `test_materialization_invariance` (the oracle asserts rows): RIGHT OUTER -> LEFT re-orientations and a FULL on `name` -> INNER + LEFT, the same tightening. tpc_h `adhoc07` grew 2380 -> 2408: the customer domain join, INNER now, gets the semi-join `IN (select ...)` filter the optimizer gives any INNER dimension join (a SQL-only test; the join supplies an output the fact already carries, the pre-existing correct-and-wasteful shape). The BigQuery FULL-join key fixture (`select name as n2, count(status) as n` over the derived-key model) lost its FULL entirely, the aggregate by `name` meeting the customer domain being LEFT now, and is re-anchored on a three-fact `union join` over derived keys whose FULLs are the declaration's own with a null-safe coalesced key. No `zquery<N>.log` moved, and the 34 other tests rendering a FULL on a coalesced key are byte-identical.

The A/B (compiled SQL over the planner suites, `94db7aa55` vs `9fff5a23a` plus the recursion guard, 3632 tests, `test_keyspace.py` ignored on the baseline since it imports the new module): 7 tests differ, and they are the four `test_duckdb_file_source` temp paths (identical lengths), gcat `test_environment` (the known column-order nondeterminism) and the two rowset tests of `test_duckdb_rowset_null_group_rejoin.py` (rows asserted). No `zquery<N>.log` moved. The first pass had TPC-DS q64 in a `RecursionError`: `cs_ui`'s body holds a membership over `cs_ui`, so computing its witness asked for its witness; a rowset in progress is an empty witness now (a body cannot witness itself), and q64 is byte-identical. So every plan reading a rowset over a `~` model (q64's `ss_rows_*`, `test_complex`, gcat) now has the rowset's regions in its keyspace and moved nothing: the unsplit boundary was already the region holder, and the split fires only for a derivation that takes a value on the padding.

### Pick up here

0. **Phases 4, 5, owner answer 5, phases 6-7 step 1 and step 2 (five chunks, 4 below is the fifth) and the rowset witness (above) are built; read their sections above before touching them.** No open budgets: thelook q22 was rebaselined (the parked flatten stays parked); TPC-DS q77 (a BETWEEN empties a region) and thelook q16 (two partial bindings have no relationship: no shard completions, no partial anchors) cleared in step 2's second and third chunks. The oracle owes one query (`select status, count(customer_id) where activity = 'dormant'`); it is the one strict xfail left. Every demanded region has a domain, so every `~` model's plan moved; the A/B against `6400a24fd` is recorded at the end of step 1.
1. **Owner answer 5 is built** (its section above). Left: a HAVING aggregate riding the mandatory list blocks the sole-output push (q41, equivalent rows).
2. A row-stream BUCKET that mixes present and absent derivations (`upper(name)` beside `status` in one BASIC group) is not split the way a ROOT bucket is. No test yet.
3. **Phase 5 is built** (its section above). What it leaves: the audit's `where` emitter is a planner log line in `region_domains.add_region_domain_buckets` ("keeps its padded plan"), firing for an atom whose inputs are neither carried nor absent-and-rows-only, a shape no suite or oracle query has; and `_preserved_final_branch` still stands beside the keyspace answer.
4. **Pin-heal per plan: BUILT (step 2, fifth chunk, `e41a06e95`).** The wart had two motivating shapes and neither was the double build itself. (a) thelook q16 was a wrong heal decision (the owner rule, third chunk). (b) TPC-DS q77's `wr_grouped` was a MISSING SEAM: `build_nested_select` materializes a fresh build environment for every rowset body / union arm / merge arm (the only `materialize_delta` caller beside `get_query_node`) and never healed it, never recorded its own references; the "anchor guard reads the whole statement's references" diagnosis was wrong, the body was never healed at all inside a statement. `partial_bridging.scope_statement` is now the one seam (authored + output addresses, `heal_pinned_partials`, `drop_excluded_partials`), called by both. q77 9162 -> 8544; the A/B over the planner suites (rows + SQL against a fresh capture of `40ff1ff7d`): every SQL move is a body or arm that carries its own WHERE healing (`test_complex` rowsets drop the `stores INNER JOIN` key completion, q29's `_sr_data` and the q77-like TVF arms scan the returns table alone, `test_property_via_partial_fk` loses a `RIGHT JOIN date_dim`), the row moves are `now()` / unordered `LIMIT` ties. Trap met on the way: a sentinel run had rewritten `zquery77.log` to the NEW plan before the baseline capture, so the baseline's q77 "grew" past that log's budget; a baseline run is only clean on committed logs. Still built twice per plan (heal's keyspace from the authored bindings, the plan's from the rewritten ones). **Whether that is safe was then AUDITED rather than argued** (sixth chunk): `v4_helper/keyspace_audit.py` under `TRILOGY_KEYSPACE_HEAL_AUDIT=<file>` builds every plan's keyspace a second time over `environment.authored_datasources` (heal records them) and logs each plan whose reader-visible facts differ (live regions and their spans, demanded / output-demanded spans, entity keys, span reach over the common in-play spans, what an output is carried on). Over the planner suites (3519 tests) the only record is `select customer_id, name where order_seq = 1`: the window feeder is a sub-plan with NO WHERE of its own, and a keyspace over the authored bindings would keep the orderless-customer region live, pad it, and `row_number` the padding row (`order_seq = 1` for a customer with no order, wrong rows against the twin). So the refactor as briefed ("one keyspace per plan, built with the plan's own outputs and conditions") is WRONG in that direction: heal is a STATEMENT fact every plan under the statement inherits, which is exactly what the rewrite gives every reader for free. The rewrite never diverged the other way (no witness moved, no live region changed). Two authored-side defects the rewrite had masked are fixed: an emptied completion still demanded its spans (`Region.live_completes`; `output_demanded_spans` reads it), and once heal stripped the last `~` the keyspace skipped canonicalization, so one entity was spelled `c2` in one plan and `customer_id` in the next (facts are read whether or not a license survives). Neither moved any SQL. Pinned in `test_keyspace.py` (`test_emptied_completion_demands_nothing`, `test_entity_is_spelled_the_same_with_and_without_a_license`, `test_sub_plan_without_the_where_inherits_the_statement_heal`). CLOSED: the readers of `Modifier.PARTIAL` keep reading the rewrite (one seam for ~50 files), the anchor guards stay model-structure walks with q83/q01/q91 behind them; the audit stays in the tree as the check to re-run if heal or the keyspace's facts change. The other parked phase 3 heal improvement, merged `~` keys, is still untried.
5. **A rowset as a witness: BUILT for placement** (section above, `9fff5a23a`..`c13fdf052`); the rowset xfail is promoted. (a) is DONE (third pass above: three rules, not one; an extent-free span pairs INNER at plan time with its complete domain, and the holder/host feeder-values vetoes read only UNPAIRED value NULLs). Left in it: (b) `tree_in_play_spans` is still the stopgap for the padding matrix (the witness could feed `in_play` in the reader's spelling instead); (c) an unsplit boundary is not stamped as the region holder (tried, grew the rollup-over-union-joined-rowsets plan); (d) a body region whose span no handle spells is not a region of the reader; (e) the heal keyspace (`partial_bridging`) takes no witnesses. The OPTIONAL entity is modelled after all, see phase 4.
9. **Handoff for a fresh session (2026-09-26, second session).** Branch `extension-row-null-semantics`, PR #702. Earlier today: `9fff5a23a` the witness, `e81790cce` the q64 guard + A/B record, `c13fdf052` the plan-shape pass, `350169565` (CI fully green); then the third plan-shape pass (5(a), the commit after `350169565`). Confirm that commit with `gh pr checks 702` before anything else. Next chunks in the order I would take them: (i) item 1 (q41's HAVING aggregate blocking the sole-output push, plan quality); (ii) item 2 needs its test first (the oracle model); (iii) 5(b)-(e). Process that held today: SQL-only capture (`ks_sqldump`) over the eight planner suites is ~17 minutes and survives as a background run; make the baseline patch with `git diff --output=` (PowerShell `>` mangles it) and `--ignore` a test file that imports a new module on the baseline run; the `_witness` in-progress placeholder is what stops q64 recursing; guards that catch a wrong turn here are `test_duckdb_nullability_matrix.py` (a ROOT domain must still be widened with a key it binds) and `test_duckdb_fuzzer_regressions.py::test_rollup_label_over_union_joined_rowsets` (stamping unsplit boundaries).
6. **Phases 6-7, step 2: delete what the contract made redundant — TRIED (fourth chunk, above).** Three entries went (the region sibling-grain widening and `kept_axis` in `_assemble_final_node`, `MergeNode`'s hidden-output shortcut rule); the rest are the fallback for regions the keyspace does not model (a composite-key `~` region, `returns` beside `sales` with no domain, an alias riding its source's bucket, two families' padding meeting in one FULL join) and stay until those are modelled, each with its shape recorded at the site. The `span_scope` fields go with their last reader; `in_play` stays for pin-heal.
7. Latent, found on the way and fixed: two merges of the same members typed differently shared a CTE (`_null_extended_sides`). The solid stream's "correct and wasteful" dimension join is FIXED (step 1: a solid group reads its spans from the fact's own column).
8. Process. Commit early, push to PR #702 and let CI run the full suite (it only triggers on pull requests; the local full suite is 2h+ and gets killed for memory, as do long idle background runs). Locally: one pytest at a time, targeted files or the ~16 minute planner suites; `tests/dialect` belongs in them (its BigQuery FULL-join key tests render planner output, and broke unnoticed for three commits when the step 2 retirement deletions made their fixture's coalesced key complete; re-anchored on the oracle model in the sixth chunk). A known CI flake, not a finding: `tests/modeling/gcat/test_gcat.py` failing as one `UnicodeDecodeError` in `duckdb_engine` followed by "Current transaction is aborted" on every later gcat test is the test's own raw-SQL `httpfs` fetch from GCS erroring; re-run the job. `tests/engine/test_bigquery.py::test_readme` with a Google `RefreshError` is the same kind. Three tests fail under `ks_rowdump` only (`test_demo_aggregates`, `test_existence`, `test_bare_aggregate_function_concepts_resolve_at_build`): they index the cursor the plugin wraps.

Owner questions 4 and 5 are answered (below); 1 and 3 were taken as proposed, 2 stays with the union machinery.

## Guards

- `tests/engine/test_derived_key_domain.py`: the oracle (materialization invariance: storing a derivation as a column at its grain must never change a query's rows), `OWED` strict xfails as targets, inline-vs-named spellings, and three traps (`?` key, ROLLUP subtotal, present entity with an unbound property).
- `tests/engine/test_duckdb_partial_key_assembly.py`, `test_duckdb_partial_fk_field_report.py`, `test_multi_fact_nullable_fk_extent.py`, `tests/optimization/test_join_upgrade.py`.
- `tests/modeling` SQL-size budgets and `zquery<N>.log` hashes: the byte-identical check for phases 1-3, still byte-identical after phase 4.
- `tests/engine/test_duckdb_rowset_null_group_rejoin.py::test_unsold_item_counts_no_lines` and `tests/core/processing/test_keyspace.py` (the `_domains` tests: which regions get a domain, what rides it hidden).
- Process notes in the handoff still apply (one pytest at a time, `-v` to a log file, never `stash`/`reset` in this tree).
- `local_scripts/keyspace_ab/`: the A/B tooling phases 3 and 4 were verified with (compiled-SQL and row capture as pytest plugins, a per-plan keyspace printer, rename-insensitive SQL and row differs, the phase-4 off switch) and the traps in using it.

## Questions for the owner

1. Is a region keyed by ENTITY keys only, or by concepts? (Entity keys is the proposal; a property is never a witness, tpc_ds q70.)
2. Is `complete where` a region fact (a partition is a region of its key) or does it stay with the union machinery?
3. Does the keyspace live on `BuildInfo` (per plan) with `DomainGraph` staying model-level, or does `DomainGraph` grow a statement overlay the way scoped joins did (`with_overlay`)?
4. `select status, count(customer_id)`: the customer key is demanded only as an aggregate ARGUMENT. Under the region model the `{customer}` region exists and the orderless customer counts under `status = NULL`, which is what the materialized twin returns. Confirm that is the intended answer, since no output carries the key.
5. `undelivered_customer <- filter name where undelivered` in a SELECT restricts the row stream today (returns only customer 1, on the materialized twin too). Under the region model it is a value, NULL where the predicate fails. Confirm, because that is a visible behaviour change outside `~` models.

### Answers (owner, 2026-09-21)

- **4: yes.** An aggregate ARGUMENT demands its key's region: `select status, count(customer_id)` counts the orderless customer under `status = NULL`, as the materialized twin does.
- **5: it is a bug, not a choice.** A filter narrows only the concept it defines, never the other columns' rows: `select customer_id, undelivered_customer` returns every customer, NULL where the predicate fails. The materialized twin restricting the stream is the same bug, so the oracle cannot judge this one; it needs its own expected-rows test. It reaches outside `~` models.

## What already exists to start from

The prototype (commits `e0f6424b7`..`d1c482137` on `extension-row-null-semantics`, backed out in the commit after them) implements phase 4 for ONE region shape and passes the oracle for it. Read it as evidence, not as a base:

- it proves the target plan renders and executes: `customers LEFT JOIN (derivation over orders) ON customer_id`, the dim-peel shape;
- its predicates are keyspace queries in embryo: `absent_on_extension`, `_null_on_padding`, `_solid_groups`, `_filters_span_domain`, `reads_rows_only`;
- its failures are the requirements list: q94 (presence is not FD), q95 (an atom the extension rows never see is a lost filter), the three folds (rows are not columns), sub-plan firing, SQL budgets tripping on NULL-propagating arithmetic.
